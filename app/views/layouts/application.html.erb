<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "BitBuddy" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <nav class="bar bar-tab">
      <a class="tab-item active" href="<%= root_url %>">
        <span class="icon icon-home"></span>
        <span class="tab-label">BitBuddy</span>
      </a>
      <a class="tab-item" class="push-right" href="<%= contacts_url %>">
        <span class="icon icon-list"></span>
        <span class="tab-label">Contacts</span>
      </a>
      <a class="tab-item" href="<%= new_coinbase_request_path %>">
        <span class="icon icon-share"></span>
        <span class="tab-label">Request Ƀ</span>
      </a>
      <a class="tab-item" href="<%= destroy_session_path %>">
        <span class="icon icon-trash"></span>
        <span class="tab-label">Sign out</span>
      </a>
    </nav>

    <div class="content">
      <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
    <script type='text/javascript'>

    </script>
  </body>
</html>
